name: build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    name: build
    steps:
    - name: build
      uses: actions/checkout@v1
    - name: build
      uses: ./.github/actions/arduino
      with:
        run-command: ".ci/build.sh"
